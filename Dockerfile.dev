# Development Dockerfile with Hot Reload Support using Spring Boot DevTools

FROM maven:3.9-eclipse-temurin-21

WORKDIR /app

# Copy only pom.xml first to leverage Docker cache for dependencies
COPY ManagerTasks-Api/pom.xml .

# Download dependencies (this layer is cached if pom.xml doesn't change)
RUN mvn dependency:go-offline -B

# Copy source code (this layer is invalidated when source changes)
COPY ManagerTasks-Api/src ./src

# Expose the application port
EXPOSE 8080

# Run Spring Boot with DevTools enabled for hot reload
# DevTools automatically restarts the app when files in /app/src change
# The application will watch for changes and automatically reload
ENTRYPOINT ["mvn", "spring-boot:run", "-Dspring-boot.run.fork=false"]
